# API base URL used by the app at runtime.
# - For local development, prefer SAME-ORIGIN + Vite proxy to avoid CORS:
#     VITE_API_BASE=
#     VITE_API_PROXY_TARGET=http://localhost:3000
# - For direct calls (no proxy), set an absolute URL (may require CORS on server):
#     VITE_API_BASE=http://localhost:3000
#
# Leave empty to use same-origin (works with the dev proxy below)
VITE_API_BASE=

# Target used by Vite dev proxy for /api requests
VITE_API_PROXY_TARGET=http://localhost:3000
